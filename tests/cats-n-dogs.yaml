version: '3'
services:
  camera:
    build: camera
    depends_on:
      - redis
    environment:
      - ANIMAL=cat
    command: ['python', './read_camera.py', '-u', 'redis://redis:6379', '--test', '${ANIMAL}.jpg']
