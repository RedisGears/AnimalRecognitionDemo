version: '3'
services:
  camera:
    build:
      context: camera
      dockerfile: Dockerfile.debian
    depends_on:
      - redis
    environment:
      - ANIMAL
    command: python ./read_camera.py -u redis://redis:6379 --test
